spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/wms}
    username: ${DATABASE_USERNAME:wms}
    password: ${DATABASE_PASSWORD:wms123}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: ${JWT_SECRET:c2VjdXJlLWp3dC1zZWNyZXQta2V5LWZvci13bXMtYXBwbGljYXRpb24tMjAyNA==}
    expiration-ms: 86400000

server:
  port: ${PORT:8080}
